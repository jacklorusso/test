import React from 'react'
import styled from 'styled-components'

const StyledComponentName = styled.tag`

`

export default class ComponentName extends React.Component {
  render () {
    return (
      <StyledComponentName />
    )
  }
}
